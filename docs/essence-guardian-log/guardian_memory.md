# Essence Guardian Memory Log

Este arquivo fornece contexto histórico ao Essence Guardian.
Cada chamada ao Guardian deve ler este arquivo antes de avaliar e adicionar uma entrada ao final.

## Regras de Uso

- **Append-only:** Nunca editar ou remover entradas anteriores
- **Breve:** Entradas devem ser curtas e factuais (1-2 frases)
- **LLM-optimized:** Formato estruturado para consumo por IA
- **Não é autoridade:** Nunca usar para justificar violações ou como fonte de permissões
- **Discretionário:** Pode pular entradas para avaliações trivialmente insignificantes

## Formato de Entrada

```markdown
### [N] YYYY-MM-DD
- **Trigger:** [agente/chamada que invocou]
- **Scope:** [arquivos/módulos/docs avaliados]
- **Contracts checked:** [lista de contratos verificados]
- **Status:** OK | Warning | Violation
- **Note:** [1 frase factual]
```

---

## Histórico

### [1] 2026-04-14
- **Trigger:** Essence Guardian evaluation request (provider-locking plan)
- **Scope:** docs/maestro/plans/2026-04-14-provider-locking-plan.md, docs/maestro/plans/2026-04-14-provider-locking-design.md
- **Contracts checked:** determinism, idempotency, immutability, configuration-hierarchy, system-default-semantics, data-auditability
- **Status:** Warning
- **Note:** Plan has architectural risks: Executor purity violation risk (provider resolution during execution), system-default semantics unclear for provider-lock, and provider persistence in mutable config JSON needs immutability guarantee clarification.

### [2] 2026-04-14
- **Trigger:** Essence Guardian evaluation request (Phase 1 implementation — ProviderResolver Core)
- **Scope:** src/core/provider_resolver.py, src/api/errors.py, tests/unit/test_provider_resolver.py
- **Contracts checked:** determinism, data-auditability, explicit-over-implicit, no-mutable-global-state, research-oriented
- **Status:** Warning
- **Note:** ProviderResolver is pure and well-tested, but introduces external-API dependency into core layer; module-structure.md and api-integration.md not updated; strategy fallback via warnings introduces non-deterministic behavior risk when strategy data is missing.

### [3] 2026-04-15
- **Trigger:** Essence Guardian evaluation request (Phase 1b — ProviderResolver corrections)
- **Scope:** src/api/provider_resolver.py, tests/unit/test_provider_resolver.py, docs/reference/module-structure.md
- **Contracts checked:** determinism, explicit-over-implicit, data-auditability, immutability, layer-boundaries
- **Status:** Warning
- **Note:** ProviderResolver correctly moved to src/api/ and returns frozen ProviderResolution dataclass; however module-structure.md not updated to reflect new file, and warnings.warn() introduces non-deterministic side-channel for fallback detection.

### [4] 2026-04-16
- **Trigger:** Essence Guardian evaluation request (Phase 3 — --resolve-providers CLI command)
- **Scope:** src/cli/bcllm_provider.py, bcllm.py (routing), src/core/module_resolver.py, src/core/mode_matrix.py
- **Contracts checked:** determinism, idempotency, immutability, configuration-hierarchy, idempotency (data), data-auditability, explicit-over-implicit, planner-executor-purity
- **Status:** OK
- **Note:** --resolve-providers implementation is contract-compliant: explicit-only trigger, idempotent per-variant, delegates to ProviderResolver, maintains planner/executor purity, proper logging and structured reporting.

### [5] 2026-04-16
- **Trigger:** Essence Guardian evaluation request (Phase 5 — Integration + E2E Tests)
- **Scope:** tests/unit/test_planner_provider_lock.py, tests/unit/test_execution_engine_provider.py, tests/unit/test_bcllm_provider.py
- **Contracts checked:** determinism, idempotency, configuration-hierarchy, planner-purity, executor-purity, data-auditability
- **Status:** Warning
- **Note:** 29 tests pass with good structure and mocking practices, but test_provider_with_empty_string_not_added has incorrect assertion (empty string is truthy in Python), and reference docs (cli-commands.md, module-structure.md) and implementation-status.md were NOT updated per mandatory documentation rule.

### [6] 2026-04-16
- **Trigger:** Essence Guardian evaluation request (Phase 6 — Documentation + ADR)
- **Scope:** adr-001-provider-locking.md, cli-commands.md, configuration-reference.md, determinism.md, module-structure.md
- **Contracts checked:** documentation-accuracy, determinism, idempotency, configuration-hierarchy, data-auditability
- **Status:** Warning
- **Note:** ADR, CLI docs, config reference, and determinism contract are accurate; module-structure.md has duplicate incorrect entries (provider_resolver.py in Core layer, variant_signature.py in Core layer).

### [13] 18-08-2026
- **Trigger:** Main agent evaluation request (targeted re-verification of the 3 gaps flagged in [12]: configuration-hierarchy.md/determinism.md per-key chains, known-issues.md audit entry)
- **Scope:** docs/contracts/configuration-hierarchy.md, docs/contracts/determinism.md, docs/status/known-issues.md, docs/architecture/adr/adr-002-cli-presentation.md, src/core/config_resolver.py, src/cli/*.py
- **Contracts checked:** configuration hierarchy, determinism, documentation accuracy
- **Status:** Warning
- **Note:** Both contract-file fixes verified correct: all 3 per-key chains in configuration-hierarchy.md now include CLI (highest-to-lowest), determinism.md's seed chain now includes CLI (lowest-to-highest, opposite notation but same relative precedence, no contradiction between the two files), and `config_resolver.py` confirmed to check `cli_value` first everywhere (resolve_seed, resolve_prompt, `_resolve_with_force_system_default`, `_resolve_cli_or_experiment`) — no behavior change, doc-only fix as claimed. However the new known-issues.md entry's own audit numbers are factually wrong: it claims "184 print() calls, only 86 ... explicitly pass file=sys.stderr ... remaining ~98"; actual current count in src/cli/*.py is 184 print() total (correct) but 113 with file=sys.stderr and 71 without (not 86/98) — an inaccuracy inside an entry whose entire point is flagging unverified compliance claims.

### [7] 2026-08-17
- **Trigger:** Essence Guardian test invocation (fictitious scenario, no real code change)
- **Scope:** Hypothetical change to src/core/execution_engine.py (ExecutionEngine writes directly to `errors` table via its own DB connection on retryable error)
- **Contracts checked:** research-oriented, determinism, logical immutability, configuration hierarchy, idempotency, data-auditability, controlled evolution, planner-executor-purity (architecture)
- **Status:** Violation
- **Note:** Direct DB writes from ExecutionEngine break the "ExecutionEngine has zero DB access" architectural boundary and bypass ResultWriter's idempotent INSERT OR IGNORE + traceability guarantees; also undocumented, violating the mandatory documentation rule.

### [8] 17-08-2026
- **Trigger:** Main agent evaluation request (Fase 0 CLI test-suite seams: DATABASE_PATH override, base_url plumbing fix)
- **Scope:** .env, .env.example, src/cli/database.py, src/api/client.py, src/core/execution_plan.py, src/core/planner.py, src/core/execution_engine.py
- **Contracts checked:** determinism, idempotency, immutability, configuration-hierarchy, data-auditability, controlled evolution
- **Status:** Warning
- **Note:** BASE_URL plumbing fix (Planner→ExecutionPlan→ExecutionEngine) correctly restores documented behavior and doesn't touch frozen data, but confirmed a real, pre-existing Idempotency-contract tension: `src/utils/variant_signature.py` SIGNATURE_FIELD_ORDER omits BASE_URL even though schema.sql documents config as "10 model-level keys" and variant_signature as "model_id + config hash", so two variants differing only by --url collide on the UNIQUE(experiment_id, variant_signature) constraint and are rejected — this now actively blocks the CLI test suite's local-stub-vs-real-API use case; also flagged known-issues.md/configuration-reference.md as not updated for this fix and DATABASE_PATH respectively.

### [9] 17-08-2026
- **Trigger:** Main agent evaluation request (second pass, same session: mode_matrix.py INVALID routing fix, variant_signature.py field-order fix re-verification, bcllm.py composite-flow atomicity + rollback)
- **Scope:** src/core/mode_matrix.py, src/utils/variant_signature.py, bcllm.py, src/db/schema.py, src/db/schema.sql, src/db/repository.py, src/cli/bcllm_experiment.py, docs/status/known-issues.md
- **Contracts checked:** determinism, idempotency, immutability, configuration-hierarchy, data-auditability, controlled evolution
- **Status:** Warning
- **Note:** variant_signature BASE_URL idempotency gap from [8] is resolved as code, but "no migration" leaves a real (documented-partially) idempotency edge case: re-adding a semantically-identical model+config to a *pre-fix* experiment now hashes differently and won't be recognized as a duplicate. Bigger finding: enabling `(Mode.INVALID, "bcllm_experiment")` in mode_matrix also reactivates `--remove-experiment`'s pre-existing unconditional hard-delete (not just `--list-experiments` as the task framed it) — known-issues.md does disclose this ("reaches its module logic") but doesn't analyze the immutability-contract tension of hard-deleting snapshots/variants/runs that were previously unreachable/dead code. Also found `known-issues.md`'s own claim "the schema doesn't declare cascade" is factually wrong: `src/db/schema.py` (the declared runtime source of truth) DOES declare `ON DELETE CASCADE` on model_variants/question_snapshots/runs' experiment_id FK and `PRAGMA foreign_keys=ON` is enabled — `src/db/schema.sql` (the "reference copy") is stale and lacks it, so the two have silently drifted; bcllm.py rollback's explicit DELETEs are consequently redundant-but-harmless rather than necessary. bcllm.py's tuple[bool,int] return change has no other callers (verified via repo-wide grep). No violation found in the two changes' core logic themselves.

### [10] 17-08-2026
- **Trigger:** Main agent evaluation request (third pass, same session: verify user's three decisions on --remove-experiment/--remove-model/--remove-run)
- **Scope:** src/cli/bcllm_experiment.py::handle_remove_experiment, src/cli/bcllm_run.py::handle_remove_run, src/core/run_finalizer.py, src/core/planner.py::_get_runs, src/cli/bcllm_execute.py, src/core/async_orchestrator.py, bcllm.py::_rollback_created_experiment, tests/unit/cli/test_remove_commands.py, tests/cli_suite/cases/run.yaml::RN-003, docs/status/known-issues.md, docs/reference/cli-commands.md
- **Contracts checked:** logical immutability, idempotency, data-auditability, configuration hierarchy, controlled evolution, research-oriented
- **Status:** Violation
- **Note:** --remove-experiment disabling is complete and sound (composite-flow rollback in bcllm.py is a separate, already-flagged, narrowly-scoped mechanism, not a backdoor); --remove-model's hard delete is genuinely FK-protected against destroying responses/errors, confirmed. But traced a real, reachable bug in --remove-run's soft delete: Planner._get_runs() only applies the status allow-list when run_ids is None — `bcllm --execute --run <removed_run_id>` bypasses it entirely (validate_filters() doesn't check status either), so a removed run with pending items gets re-executed and AsyncOrchestrator's unconditional RunFinalizer.finalize_run() call overwrites status='removed' back to an execution-outcome status, silently reactivating it. This directly contradicts docs/status/known-issues.md's own claim ("verified, not just assumed") and docs/reference/cli-commands.md's unconditional "never picked up by --execute again" — both false for explicit --run targeting. Test suite (test_remove_commands.py, RN-003) only covers the implicit/default discovery path, not this one; RN-003's own comment admits the positive e2e scenario isn't actually expressible in the current runner.

### [11] 17-08-2026
- **Trigger:** Main agent evaluation request (fourth pass, same session: verify the `AND status != 'removed'` one-line fix to `Planner._get_runs()`'s run_ids branch, from finding [10])
- **Scope:** src/core/planner.py::_get_runs/build_plan, src/cli/bcllm_run.py::handle_remove_run docstring, src/cli/bcllm_execute.py, src/db/repository.py, src/core/run_finalizer.py, tests/unit/cli/test_remove_commands.py, tests/test_retry_whitelist_and_rerun.py, docs/status/known-issues.md, docs/reference/cli-commands.md
- **Contracts checked:** logical immutability, idempotency, data-auditability, configuration hierarchy, controlled evolution
- **Status:** OK
- **Note:** Fix confirmed sufficient and correctly placed: `Planner.build_plan()` (src/cli/bcllm_execute.py:333) is the sole caller of `build_plan`, which is the sole caller of `_get_runs` (planner.py:171) — no third path into an ExecutionPlan exists; other `runs`-table queries (`RunRepository.list_by_experiment`/`list_pending`) are unrelated to the execution/planning path (list_by_experiment intentionally still shows 'removed' rows for `--list-runs` audit purposes, list_pending is unused dead code) so the fix has no unintended side effect. Corrected prose in known-issues.md, cli-commands.md, and the handle_remove_run/`_get_runs` docstrings all check out factually against current code — no overclaiming found. New regression test and existing introspection tests (test_retry_whitelist_and_rerun.py) remain consistent with the fixed source.

### [12] 18-08-2026
- **Trigger:** Main agent evaluation request (CLI Typer/Rich migration, Fase 1 — contract/ADR groundwork, no src/ code touched)
- **Scope:** docs/architecture/adr/adr-002-cli-presentation.md, docs/contracts/interaction-contracts.md (Section 2), docs/contracts/README.md, docs/architecture/adr/000-template.md
- **Contracts checked:** research-oriented, determinism, logical immutability, configuration hierarchy, idempotency, data-auditability, controlled evolution
- **Status:** Warning
- **Note:** ADR-002's factual claims (module_resolver.py's `--questions` gap in ADD_ACTION_FLAGS/PRIORITY_FLAGS, bcllm.py's always-reraising IntegrityError handler, config_resolver.py checking cli_value first everywhere, `--output` being a dead flag per known-issues.md) all verified accurate against current code; interaction-contracts.md Section 2 is specific, verifiable, and self-scopes clearly against "no inference" (explicit NORMATIVE marker vs placeholder sections, explicit visual-design-out-of-scope carve-out). Two gaps found: (1) Decision 4 says the configuration-hierarchy.md per-key chain correction (seed/prompt/model-param omitting CLI) is "to make during this phase," but the file is unchanged (per-key chains at lines 115/132/148 still omit CLI, last-validated still 2026-04-11) — not delivered despite the ADR's own wording; (2) the ADR's own Decision-4 text also names determinism.md's seed chain as having the identical CLI-omission gap, but only configuration-hierarchy.md is listed in Related Documents as pending correction — determinism.md's gap is undiagnosed/untracked anywhere (not in known-issues.md, not in the ADR's own follow-up list).

### [14] 19-08-2026
- **Trigger:** Main agent evaluation request (Fase 3, item 5 - "double-wrapped meta" question_snapshot payload bug fix)
- **Scope:** src/core/question_loader.py::build_question_snapshot_payload, src/cli/bcllm_experiment.py::_create_question_snapshots, src/cli/bcllm_questions.py::handle_add_questions, tests/unit/core/test_question_snapshot_payload.py, tests/unit/core/test_planner.py::test_planner_recognizes_vision_question_via_canonical_payload_builder, tests/unit/cli/test_question_snapshot_equivalence.py, docs/status/known-issues.md
- **Contracts checked:** immutability, data-auditability, determinism, idempotency, configuration-hierarchy
- **Status:** Warning
- **Note:** Fix is sound and correctly scoped - canonical builder used by both call sites, idempotency skip-check untouched, ensure_ascii change confirmed cosmetic-only (repo-wide grep: every question_payload reader uses json.loads/json_extract, never text/hash comparison), non-migration of old snapshots is contract-required by immutability (not just permitted), and the "left unchanged" test genuinely proves it (inserts pre-shaped double-wrapped JSON via SnapshotFactory bypassing the new function, reads back byte-identical). Residual gap: already-persisted composite-flow snapshots created before this fix keep silently misreporting has_image=False on any future resume/re-execution, and neither the fix nor known-issues.md provides a detection query or remediation guidance for which existing experiments/snapshots are affected - an actionable audit gap left open, not merely a cosmetic format divergence.
- **Resolution (2026-08-19, user decision):** the data-auditability Warning above is closed per `docs/architecture/adr/adr-003-pre-production-data-scope.md` — the database has never held a real experiment, so no row can currently be in the affected state; no detection/remediation was built. This does not relax immutability/auditability/isolation, and expires before the system's first real experiment — see the ADR for the full reasoning.

### [15] 19-08-2026
- **Trigger:** Main agent evaluation request (Fase 3 — `null_semantics.py` → `special_config_values.py` rename + SUPPORTED/FORBIDDEN/NOT_APPLICABLE eligibility redesign for `system-default`)
- **Scope:** src/core/special_config_values.py, src/core/argv_utils.py, src/cli/bcllm_experiment.py, src/cli/bcllm_model.py, src/cli/bcllm_run.py, src/cli/bcllm_provider.py, src/cli/param_types.py, src/core/config_resolver.py, docs/contracts/system-default-semantics.md, tests/unit/cli/test_bcllm_model_system_default.py, tests/integration/test_cli_special_config_values.py
- **Contracts checked:** configuration hierarchy, determinism, idempotency, data-auditability, controlled evolution, documentation accuracy
- **Status:** Warning
- **Note:** Core mechanism is sound (config_resolver.py's FORCE_SYSTEM_DEFAULT handling for seed/prompt/reasoning/provider confirmed correct against the contract; the 8-flag bcllm_model.py fix and --url FORBIDDEN/exit-2 change confirmed accurate and tested). Found two real gaps the session's own description didn't surface: (1) `--experiment` (required=True identity flag) is FORBIDDEN in bcllm_experiment.py but left unclassified (NOT_APPLICABLE) in bcllm_model.py, bcllm_run.py, and bcllm_provider.py — `--experiment system-default` in those three modules falls through to `get_by_name("system-default")` and returns a misleading "Experiment not found" (exit 1) instead of the clean "does not accept system-default" usage error (exit 2) the contract's own rationale (lines 36/91 of system-default-semantics.md) says identity flags must get; no test covers this, and the one existing test that touches the question explicitly says "unlike the real bcllm_experiment.py, where it's FORBIDDEN" without extending the same logic to the other three modules. (2) Mandatory documentation rule gap: `docs/reference/module-structure.md` (2 lines) and `CLAUDE.md` itself still reference the old `null_semantics.py` filename as current, not updated for the rename; the rewritten `docs/contracts/system-default-semantics.md` also self-contradicts by still citing `src/core/null_semantics.py` as the "Implementation" (line 116) despite being the normative doc for this exact change.

### [16] 19-08-2026
- **Trigger:** Main agent evaluation request (composite `--create-experiment`+`--add-*` flow: real `BEGIN IMMEDIATE` transaction via new `UnitOfWork`, replacing the old compensating-DELETE rollback)
- **Scope:** src/db/unit_of_work.py (new), src/db/repository.py (4 `save()` methods: Experiment/Variant/Snapshot/Run), bcllm.py (`_handle_composite_flow` restructured, `cli_main()`/`_bootstrap_environment()` split), src/cli/bcllm_experiment.py, bcllm_model.py, bcllm_questions.py, bcllm_run.py (parse/action/adapter `commit`+`request=` passthrough), setup.py, tests/unit/db/test_unit_of_work.py, tests/unit/cli/test_composite_flow_rollback.py
- **Contracts checked:** idempotency, immutability, configuration hierarchy, determinism, data-auditability, controlled evolution, research-oriented
- **Status:** Warning
- **Note:** All claims independently verified against code and tests, no violation found: the `ResponseRepository`/`ResultWriter`/`ExecutionEngine` boundary genuinely holds (repo-wide grep confirms zero `UnitOfWork`/`commit=False`/`commit: bool` references outside the 6 files that should have them; `ResponseRepository.save`/`update_manual_answer` unchanged, still unconditional immediate commit); the TOCTOU-rollback and exit-code behavior changes are exactly as disclosed and are genuine improvements, not contract erosion (seed's early pure-parse-phase check is a format-only check via the same `parse_seed_value_strict` used later for real hierarchy resolution — hierarchy itself is untouched); `UnitOfWork` fails safe (rollback-by-default, `assert_active()` guard) and its own busy-database/rollback-failure tests use real contention/proxy-connection injection, not mocks. One documentation gap: `docs/reference/module-structure.md` was not updated to list the new `src/db/unit_of_work.py` file (design/investigation/known-issues status docs are thorough, but the canonical current-implementation-state reference doc lags, same pattern as entry [15]).

### [17] 20-08-2026
- **Trigger:** Main agent evaluation request (Checkpoint A — Randomization Seed vocabulary separation from not-yet-implemented Model Seed; Planner correctness bug fix)
- **Scope:** src/core/config_resolver.py (resolve_randomization_seed/_for_run/parse_randomization_seed_strict/build_experiment_config_dict/build_run_config_dict), src/core/planner.py (_resolve_randomization_seed_effective/_normalize_randomization_seed_value), src/core/execution_plan.py, src/core/execution_engine.py, src/core/async_orchestrator.py, src/cli/bcllm_run.py, src/cli/bcllm_experiment.py, tests/unit/core/test_planner.py (2 new regression tests), docs/contracts/{determinism,immutability,configuration-hierarchy,data-auditability,system-default-semantics}.md, docs/reference/{cli-commands,configuration-reference,database-schema}.md, docs/status/{known-issues,roadmap,implementation-status,seed-vocabulary-separation-investigation}.md
- **Contracts checked:** determinism, immutability, configuration hierarchy, idempotency, data-auditability, controlled evolution, research-oriented, documentation accuracy
- **Status:** OK
- **Note:** All claims independently verified against current code, not just the description: zero remaining aliases/dead names in src/ (RUN_RESPONSES_SEED/RANDOM_SEED/--seed/seed_effective/AddRunRequest.seed all fully renamed, confirmed via repo-wide grep, only historical docstring/investigation-doc mentions remain); the real Planner bug is genuinely fixed — `_resolve_randomization_seed_effective` now requires the key present, raises `PlannerValidationError` otherwise, takes no `experiment_row`, so no fallback path exists after Run creation, matching immutability.md's "Run configuration is frozen" and determinism.md's "same config → same requests"; `resolve_randomization_seed_for_run` correctly distinguishes `None` (nothing configured, stays None) from `AUTO` (resolves once, deterministically, via SHA-256 of experiment_id:run_id) and never consults `.env`; both new regression tests (`test_planner_reads_run_own_none_randomization_seed_without_falling_back_to_experiment`, `test_planner_raises_clear_error_when_run_missing_randomization_seed_key`) exist and assert exactly the claimed scenarios; ExecutionEngine uses explicit `is not None` (never truthy) check on `randomization_seed_effective` per determinism.md's own violation-example; all listed contract/reference docs cross-checked and consistent with each other and with code (RANDOMIZATION_SEED used uniformly in .env, .env.example, cli-commands.md, configuration-reference.md, database-schema.md, system-default-semantics.md). No violation or gap found; Checkpoint B (Model Seed) correctly out of scope and not touched.

### [18] 20-08-2026
- **Trigger:** Main agent evaluation request (Checkpoint B — Model Seed `MODEL_SEED`/`--model-seed` + single canonical request-payload centralization, closed/completed work review)
- **Scope:** src/api/request_payload.py (new), src/api/client.py, src/core/execution_engine.py, src/core/config_resolver.py, src/core/execution_plan.py, src/core/planner.py, src/utils/variant_signature.py, src/cli/bcllm_model.py, src/cli/bcllm_experiment.py, bcllm.py (`_ACTION_RELEVANT_FLAGS`), src/core/result_writer.py, tests/unit/core/test_request_fidelity.py, tests/integration/test_seed_independence.py, docs/contracts/{configuration-hierarchy,determinism,immutability}.md, docs/reference/{configuration-reference,cli-commands,module-structure}.md
- **Contracts checked:** determinism, immutability, configuration hierarchy, idempotency, data-auditability, controlled evolution, research-oriented, documentation accuracy
- **Status:** OK
- **Note:** All claims independently verified against current code and tests, not just the description: `build_chat_completion_payload` is called exactly once per attempt in `ExecutionEngine.api_call_with_retry` and the same dict object flows to both `request_json` and `OpenRouterClient.chat_completion(payload=...)` (client never reconstructs, reads only `payload["model"]`/`["messages"]`, does `json=payload` directly); `model_seed`/`debug_enabled` both use explicit `is not None`/`is True` checks so `0`/off-by-default are never silently dropped or enabled, confirmed by `test_model_seed_zero_sent_not_omitted` and `test_debug_off_key_absent_from_both`; secrets (API key/Authorization header) confirmed absent from `request_json` and the sent JSON body via a real assertion, present only in `httpx` headers; the two-tier MODEL_SEED resolution (`_resolve_with_force_system_default` at Experiment level, CLI>.env>None; `_resolve_cli_or_experiment` at variant level, CLI>Experiment>None, no `.env`) is byte-for-byte the same helper/pattern already used for PROVIDER, not a new one-off; `ModelConfig` is `@dataclass(frozen=True)` and `VariantRepository` has only `save()`, no update method, so a variant's `MODEL_SEED` genuinely cannot change post-creation; `test_seed_independence.py` is a real Planner+ExecutionEngine+in-memory-SQLite integration test (not ExecutionEngine-only), and repo-wide grep confirms `model_seed` appears only in the 8 expected files with zero leakage into `randomizer.py`/`result_writer.py`/`async_orchestrator.py`; `variant_signature.py`'s `value == ""` skip-check does not falsely treat `model_seed=0` as unset (`0 == ""` is `False` in Python), verified by reading the code directly, not assumed. One documentation gap found (same recurring pattern as memory entries [15]/[16]): `docs/reference/module-structure.md` was not updated for either the new `src/api/request_payload.py` file or `OpenRouterClient.chat_completion`'s signature change from ~13 scalar kwargs to `(payload, base_url)` — the canonical current-implementation-state reference doc lags behind design/status docs again.

### [19] 20-08-2026
- **Trigger:** Main agent evaluation request (Checkpoint C — logging/observability/auditability architecture: `src/utils/log_events.py`, `log_emitter.py`, `redaction.py` new; `operation_id` correlation threaded through the whole `--execute` pipeline; TRACE-tier request/response logging; uniform `KeyboardInterrupt` handling)
- **Scope:** src/utils/{log_events,log_emitter,redaction,logging_config}.py, src/core/{planner,execution_plan,execution_engine,retry,async_orchestrator,run_finalizer,async_writer,result_writer,config_resolver}.py, src/api/client.py, bcllm.py, src/cli/bcllm_execute.py, docs/contracts/{interaction-contracts,data-auditability,idempotency,determinism,immutability}.md, docs/reference/{configuration-reference,module-structure}.md, docs/status/known-issues.md
- **Contracts checked:** determinism, idempotency, immutability, configuration hierarchy, data-auditability, controlled evolution, research-oriented
- **Status:** Warning
- **Note:** Independently re-verified (fresh, not resuming the cut-off prior attempt): `emit_event` genuinely is the sole emission path (severity floor structurally enforced, redaction unconditional, try/except never propagates); `operation_id` confirmed threaded through the full documented chain (ExecutionPlan is a frozen field, explicitly documented as never persisted to DB) via direct grep of every named module, no globals/contextvars; TRACE-tier `REQUEST_PAYLOAD_TRACE`/`UPSTREAM_ECHO_TRACE`/`STREAM_CHUNK_TRACE` read `request_json`/`payload`/`raw_response` but never write back to them — `emit_event`'s `redact()` returns a copy, the persisted `request_json`/`raw_response` fields are serialized from the original unredacted objects before/independent of the TRACE calls; `CONFIG_RESOLVED`/`INHERITANCE_DECISION` calls happen strictly after resolution using the already-computed value, no feedback path into resolution logic; `LOG_PROFILE` confirmed `.env`-only with zero CLI flag anywhere (module_resolver.py grep empty) matching docs; determinism.md §7's "no `payload_fingerprint` field exists" and idempotency.md's "logging failure never implies not-attempted" claims both verified against code/tests (`test_write_failure_does_not_cause_duplicate_result_semantics` exists as cited); known-issues.md's "7 of 9 CLI modules" KeyboardInterrupt-gap figure verified correct via full grep (bcllm_execute.py and bcllm_review.py both already had ad-hoc handlers pre-checkpoint) — more accurate than the task-framing's "8 modules" claim. One real, undisclosed gap found, adjacent to but not part of this checkpoint's own file list: `bcllm_review.py`'s pre-existing `KeyboardInterrupt` handlers (lines 90/112, untouched by this checkpoint) return exit code 0 and emit no structured event at all on Ctrl-C, which (a) contradicts `interaction-contracts.md` §2's own exit-code rule ("`130`... Any future long-running command that can be interrupted must follow the same convention rather than inventing a new code") and (b) means `--review` sessions interrupted by Ctrl-C produce zero `COMMAND_INTERRUPTED`/correlation evidence, undermining this checkpoint's own "operational auditability" framing for that one command family; not tracked anywhere in known-issues.md despite being directly adjacent to the "uniform KeyboardInterrupt handling" claim just added there.

### [20] 20-08-2026
- **Trigger:** Main agent evaluation request (narrow re-check: `--remove-question` complete removal, closing gap flagged in the prior marco-4A review)
- **Scope:** src/cli/commands/questions.py, src/cli/bcllm_questions.py, src/core/module_resolver.py, src/db/repository.py, src/utils/log_events.py, src/cli/bcllm_main.py, docs/contracts/immutability.md, tests/unit/cli/test_remove_question_removed.py, tests/cli_suite/cases/questions.yaml::AQ-006
- **Contracts checked:** logical immutability, data-auditability (docs-mandate), controlled evolution
- **Status:** OK
- **Note:** Removal confirmed genuinely complete via direct code read, not just the task's own claims: `SnapshotRepository` has no `delete()` method at all (repository.py lines 271-276), `--remove-question` absent from `module_resolver.py`'s `_MODULE_MAP`/`ADD_ACTION_FLAGS`/`PRIORITY_FLAGS` (fallback scan only matches `_MODULE_MAP` entries, so `resolve_module` genuinely returns `None`), `QUESTION_REMOVED` absent from `log_events.py` source (only historical doc/comment mentions remain), `bcllm_main.py` has zero matches for the flag in either curated help text or argparse declaration, and `immutability.md` §1 contains the exact "Enforcement (2026-08-20)"/"No public action anywhere..." wording the new test asserts verbatim. `AQ-006` and all 19 tests in `test_remove_question_removed.py` read accurate against current code, no overclaiming found. No violation or residual path found.

### [22] 21-08-2026
- **Trigger:** Main agent evaluation request (fast follow-up verification of the 2 findings from same-day reasoning-exclusivity review: `.env`-both-set gap fix + docs lag)
- **Scope:** src/core/config_resolver.py (`_resolve_reasoning_pair`, `build_experiment_config_dict`'s new `raise ValueError`, `build_model_config_dict`), src/core/execution_engine.py (REASONING_CONFLICT comment), tests/test_config_resolver.py::TestReasoningModeSuppression, docs/reference/cli-commands.md, docs/reference/configuration-reference.md, docs/contracts/system-default-semantics.md, docs/status/known-issues.md
- **Contracts checked:** determinism, configuration hierarchy, documentation accuracy
- **Status:** Warning
- **Note:** Gap genuinely closed and correctly scoped — raise fires only when neither CLI flag is concrete (traced all mode-suppression branches by hand, no false-positive path found for a mixed CLI+.env case), `build_model_config_dict` deliberately left unguarded as claimed (exp_config from a post-fix experiment can structurally never carry both fields non-null), `execution_engine.py`'s corrected comment holds under manual trace. New tests (`test_env_both_fields_set_with_no_cli_override_is_rejected`, `test_env_effort_plus_cli_tokens_is_rejected`, `test_env_only_one_field_set_is_not_rejected`) exercise exactly the described scenarios and assert the right outcomes despite the second test's misleading name. One new inaccuracy found in `docs/reference/configuration-reference.md`'s new paragraph: it claims the exclusivity rule "also applies when one is set via .env and the other via CLI at --add-model time," but `build_model_config_dict` never reads `.env` at all (by design, per its own docstring and CLAUDE.md) — at `--add-model` time the sibling source is experiment-inherited config, not `.env` directly; `docs/contracts/system-default-semantics.md`'s own wording is accurate and does not make this claim. Could not independently run `pytest` (no Bash tool available in this session) — baseline count claim not independently re-verified, only read.

### [21] 20-08-2026
- **Trigger:** Main agent evaluation request (CLI Typer migration marco 4B, second slice — bcllm_model.py argparse→Typer conversion)
- **Scope:** src/cli/commands/model.py, src/cli/bcllm_model.py, src/cli/param_types.py, src/core/special_config_values.py, src/core/module_resolver.py, src/cli/bcllm_experiment.py (`_add_models_at_creation`), tests/unit/cli/test_commands_model.py, test_model_logging_events.py, test_model_standalone_composite_parity.py, tests/cli_suite/runner/coverage.py, docs/status/known-issues.md, docs/status/cli-output-classification.md, docs/reference/module-structure.md
- **Contracts checked:** configuration hierarchy, system-default semantics, determinism, immutability, data-auditability, controlled evolution, documentation accuracy
- **Status:** OK
- **Note:** All claims independently verified against code, not just the description. `--reasoning`'s dual choices+system-default design confirmed faithful to the old argparse behavior: `ReasoningEffort` Enum rejects `'null'`/any invalid string at Click's own choice-validation layer (exit 2, `test_legacy_null_rejected`/`test_invalid_value_rejected` both assert `.status == 2`), `'none'` is a real enum member distinct from Python `None` (`reasoning is None` only true when the flag is absent), `'system-default'` converts to `FORCE_SYSTEM_DEFAULT` only inside the command body after enum validation already passed — matches `typer_reject_special_values`'s FORBIDDEN pattern used correctly for `--experiment`/`--url`/`--add-model`/`--remove-model` (the entry-[15] `--experiment` FORBIDDEN gap is not regressed here). `Event.MODEL_ADDED` no-double-emission claim verified via a real, non-mocked test (`test_model_logging_events.py::test_composite_flow_emits_exactly_one_event_per_model`) that invokes the actual `bcllm._handle_composite_flow` entry point and asserts exactly one JSONL event — independently confirmed via `module_resolver.py` that `--add-model` membership in `ADD_ACTION_FLAGS` makes `bcllm_experiment.py::_add_models_at_creation`'s own `emit_event` call structurally unreachable through the real CLI (composite flow always intercepts before any module dispatch when both `--create-experiment` and `--add-model` are present). MODEL_SEED zero-vs-None-vs-system-default confirmed correct (`getattr(cli_args, 'model_seed', None)` alone, no falsy-collapsing `or`, real DB-backed test asserts `config["MODEL_SEED"] == 0` and `is not None`). `--remove-model` hard-delete confirmed unchanged (docstring/help-text wording corrected only, `var_repo.delete()` call untouched). One pre-existing, unmodified-by-this-slice risk noted for the record (not a new finding): `config_resolver.py::build_model_config_dict` resolves `MODEL_MAX_TOKENS_REASONING` via `getattr(cli_args, 'reasoning_tokens', None) or getattr(cli_args, 'max_reasoning', None)` — if `reasoning_tokens` is explicitly `0` or `FORCE_SYSTEM_DEFAULT` (both falsy), the `or` silently falls through to `max_reasoning` instead of honoring the explicit value, a latent determinism/config-hierarchy risk in code this slice did not touch. Mandatory-documentation-rule fully satisfied: known-issues.md, cli-output-classification.md, and module-structure.md all updated and independently confirmed accurate against current code.
